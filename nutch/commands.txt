
gsutil -m cp -r gs://sangraha/setu/nutch .

chmod +x -R nutch

PATH=$PATH:~/nutch/bin crawl \
    -D http.agent.name=fetchers \
    -D yarn.scheduler.maximum-allocation-vcores=16 \
    -s gs://sangraha/setu/nutch/seed.txt \
    --num-tasks 16 \
    --num-fetchers 16 \
    gs://sangraha/setu/nutch/trials/2 \
    10